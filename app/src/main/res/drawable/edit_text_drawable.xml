<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
        <solid android:color="@android:color/transparent" /> <!-- Background color -->

        <!-- Border -->
        <stroke
            android:width="2dp"
            android:color="#53FFFFFF" /> <!-- Border color -->

        <!-- Corner radius for curved borders -->
        <corners android:radius="12dp" />

        <!-- Padding inside the EditText -->
        <padding
            android:top="15dp"
            android:bottom="15dp"
            android:left="10dp"
            android:right="10dp" />
</shape>

